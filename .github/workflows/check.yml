name: Check  
run-name: Check $${{ github.ref }} 
on:
  pull_request:
    branches: 
      - main
    types: 
      - opened
      - reopened
      - edited
      - synchronize 

jobs:
  Format repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Running Black for formatting 🧹"
      - uses: rickstaa/action-black@latest
        with:
          black_args: --check --diff .
      - run: echo "Checks finished 🏁"